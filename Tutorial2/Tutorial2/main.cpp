#include "HelloGL.h"

int main(int argc, char* argv[])
{
	HelloGL* game = new HelloGL(argc, argv); //creates a new game instance of the HelloGL class

	return 0; //successful exit if game instance is created
}